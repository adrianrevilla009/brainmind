apiVersion: 1

providers:
  - name: BrainMind
    orgId: 1
    folder: BrainMind
    folderUid: brainmind
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
